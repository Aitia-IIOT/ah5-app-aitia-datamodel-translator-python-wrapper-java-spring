# ah5-app-aitia-datamodel-translator-python-wrapper-java-spring

This project contains two Application systems providing the following functionalities:
1. *DataModelTranslationWrapper*: Provides a dataModelTranslation service, where the translation is performed using a script from an outer source.
2. *DataSaver*: Saves the input data into the configured directory.
